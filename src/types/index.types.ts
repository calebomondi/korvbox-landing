export interface Stats {
    total_amount_deposited: number;
    total_users: number;
    total_vaults: number;
    average_deposit_per_user: number;
    top_depositor: string;
    top_depositor_amount: number;
}